# ADR template — Data migration

- Status, owner, date, from/to schema and minimum compatible build:
- Immutable released-version fixtures and data inventory:
- Alternatives, including read-only refusal:
- Validation, staged atomic write, flush, backup, and integrity checks:
- Unknown future schema and corrupt/partial records:
- Profile/DPAPI portability and re-authentication implications:
- Failure injection at each write boundary:
- Rollback compatibility and explicit recovery UX:
- Privacy/security review of transformed fields:
- Golden-fixture, restart, and clean-VM evidence:
- Required approvers: storage, security, QA, and release:
